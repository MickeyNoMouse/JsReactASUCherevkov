/*import React from "react";

function Button({ onClick, children}) {
    return (
        <button onClick={onClick} style={{backgroundColor:'aqua', color: 'blue', fontSize:'15px', fontWeight: 'bold'}} >
            {children}
        </button>
    );
}
export default Button;
*/